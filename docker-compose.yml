version: '2'
services:
    oled:
        restart: always
        build: ./oled
        privileged: true
    motor:
        restart: always
        build: ./motor
        privileged: true
        ports:
            - "80:5000"        
    camera:
        restart: always
        build: ./camera
        privileged: true
